# T-800: Módulo de aquisição de alvos. YouTube.
from discord.ext import commands
from data.data_manager import DataManager

class YouTubeCommands(commands.Cog):
    def __init__(self, bot):
        self.bot = bot
        self.data_manager = DataManager()

    @commands.command(name="addyoutube")
    async def add_youtube_user(self, ctx, youtube_username: str):
        # Adicionando um novo alvo do YouTube à memória.
        user_id = ctx.author.id
        self.data_manager.add_user(user_id, youtube_name=youtube_username)
        await ctx.send(f"Alvo '{youtube_username}' adicionado à lista de vigilância do YouTube. Eu voltarei a verificar.")

def setup(bot):
    bot.add_cog(YouTubeCommands(bot))
